/**
 * App.jsx
 * -------
 * Root component. Currently renders the Home page directly.
 * Extend this for multi-page routing (react-router) if needed later.
 */

import React from 'react';
import Home from './pages/Home';

export default function App() {
  return <Home />;
}
